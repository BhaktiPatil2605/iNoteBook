import { createContext } from "react";

const noteContext=createContext();

export default noteContext;

// these file is used to create only context like i am just creating a context here just to use it and implement it 